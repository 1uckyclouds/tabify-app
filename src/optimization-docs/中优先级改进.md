# 🚀 中优先级UI优化建议

## 📋 概述
这些改进将显著提升用户体验和功能完整性，建议在高优先级完成后实施。主要涉及功能增强、状态管理和交互优化。

---

## 1. 🔍 增强搜索功能

### 问题描述
当前搜索功能较为基础，缺乏高级搜索选项和搜索历史。

### 技术实现

#### 1.1 添加搜索建议和历史
```typescript
// hooks/useSearch.ts
import { useState, useEffect, useMemo } from 'react';

interface SearchHistory {
  query: string;
  timestamp: number;
  resultCount: number;
}

export const useSearch = () => {
  const [searchQuery, setSearchQuery] = useState('');
  const [searchHistory, setSearchHistory] = useState<SearchHistory[]>([]);
  const [showSuggestions, setShowSuggestions] = useState(false);

  // 从localStorage加载搜索历史
  useEffect(() => {
    const saved = localStorage.getItem('tabify-search-history');
    if (saved) {
      setSearchHistory(JSON.parse(saved));
    }
  }, []);

  // 保存搜索历史
  const saveSearch = (query: string, resultCount: number) => {
    const newHistory = [
      { query, timestamp: Date.now(), resultCount },
      ...searchHistory.filter(h => h.query !== query).slice(0, 9)
    ];
    setSearchHistory(newHistory);
    localStorage.setItem('tabify-search-history', JSON.stringify(newHistory));
  };

  // 搜索建议
  const suggestions = useMemo(() => {
    if (!searchQuery.trim()) return [];
    return searchHistory
      .filter(h => h.query.toLowerCase().includes(searchQuery.toLowerCase()))
      .slice(0, 5);
  }, [searchQuery, searchHistory]);

  return {
    searchQuery,
    setSearchQuery,
    searchHistory,
    suggestions,
    showSuggestions,
    setShowSuggestions,
    saveSearch
  };
};
```

#### 1.2 搜索建议下拉组件
```typescript
// components/SearchSuggestions.tsx
import React from 'react';
import { SearchHistory } from '../hooks/useSearch';

interface SearchSuggestionsProps {
  suggestions: SearchHistory[];
  onSelect: (query: string) => void;
  onClose: () => void;
}

export const SearchSuggestions: React.FC<SearchSuggestionsProps> = ({
  suggestions,
  onSelect,
  onClose
}) => {
  if (suggestions.length === 0) return null;

  return (
    <div className="absolute top-full left-0 right-0 bg-white border border-gray-200 rounded-lg shadow-lg mt-1 z-50">
      <div className="py-2">
        <div className="px-3 py-1 text-xs text-gray-500 font-medium border-b border-gray-100">
          搜索历史
        </div>
        {suggestions.map((suggestion, index) => (
          <button
            key={index}
            className="w-full px-3 py-2 text-left hover:bg-gray-50 flex items-center justify-between group"
            onClick={() => onSelect(suggestion.query)}
          >
            <div className="flex items-center gap-2">
              <i className="ri-history-line text-gray-400 text-sm"></i>
              <span className="text-sm text-gray-700">{suggestion.query}</span>
            </div>
            <span className="text-xs text-gray-400 opacity-0 group-hover:opacity-100 transition-opacity">
              {suggestion.resultCount} 个结果
            </span>
          </button>
        ))}
      </div>
    </div>
  );
};
```

#### 1.3 高级搜索过滤器
```typescript
// components/SearchFilters.tsx
import React from 'react';

interface SearchFiltersProps {
  filters: {
    domain: string;
    dateRange: string;
    tabType: string;
  };
  onFiltersChange: (filters: any) => void;
}

export const SearchFilters: React.FC<SearchFiltersProps> = ({
  filters,
  onFiltersChange
}) => {
  return (
    <div className="flex gap-2 mt-2">
      <select 
        className="px-3 py-1 text-sm border border-gray-300 rounded-md bg-white"
        value={filters.domain}
        onChange={(e) => onFiltersChange({...filters, domain: e.target.value})}
      >
        <option value="">所有域名</option>
        <option value="github.com">GitHub</option>
        <option value="stackoverflow.com">Stack Overflow</option>
        <option value="google.com">Google</option>
      </select>
      
      <select 
        className="px-3 py-1 text-sm border border-gray-300 rounded-md bg-white"
        value={filters.dateRange}
        onChange={(e) => onFiltersChange({...filters, dateRange: e.target.value})}
      >
        <option value="">任何时间</option>
        <option value="today">今天</option>
        <option value="week">本周</option>
        <option value="month">本月</option>
      </select>
    </div>
  );
};
```

---

## 2. 📊 改进状态指示器

### 问题描述
用户无法清楚了解当前操作状态和系统反馈。

### 技术实现

#### 2.1 加载状态组件
```typescript
// components/LoadingStates.tsx
import React from 'react';

// 骨架屏加载
export const TabItemSkeleton: React.FC = () => (
  <div className="p-4 border border-gray-200 rounded-xl bg-white animate-pulse">
    <div className="flex items-start gap-3">
      <div className="w-5 h-5 bg-gray-200 rounded-sm flex-shrink-0"></div>
      <div className="flex-1 min-w-0">
        <div className="h-4 bg-gray-200 rounded w-3/4 mb-2"></div>
        <div className="h-3 bg-gray-200 rounded w-1/2"></div>
      </div>
      <div className="flex gap-1">
        <div className="w-6 h-6 bg-gray-200 rounded"></div>
        <div className="w-6 h-6 bg-gray-200 rounded"></div>
      </div>
    </div>
  </div>
);

// 搜索加载指示器
export const SearchLoading: React.FC = () => (
  <div className="flex items-center justify-center py-8">
    <div className="flex items-center gap-2 text-gray-500">
      <div className="w-4 h-4 border-2 border-blue-500 border-t-transparent rounded-full animate-spin"></div>
      <span className="text-sm">搜索中...</span>
    </div>
  </div>
);

// 空状态组件
export const EmptyState: React.FC<{ 
  icon: string; 
  title: string; 
  description: string;
  action?: React.ReactNode;
}> = ({ icon, title, description, action }) => (
  <div className="flex flex-col items-center justify-center py-12 text-center">
    <i className={`${icon} text-4xl text-gray-300 mb-4`}></i>
    <h3 className="text-lg font-medium text-gray-900 mb-2">{title}</h3>
    <p className="text-gray-500 mb-4 max-w-sm">{description}</p>
    {action}
  </div>
);
```

#### 2.2 操作反馈Toast组件
```typescript
// components/Toast.tsx
import React, { useEffect, useState } from 'react';

interface ToastProps {
  type: 'success' | 'error' | 'warning' | 'info';
  message: string;
  duration?: number;
  onClose: () => void;
}

const toastStyles = {
  success: 'bg-green-50 border-green-200 text-green-800',
  error: 'bg-red-50 border-red-200 text-red-800',
  warning: 'bg-yellow-50 border-yellow-200 text-yellow-800',
  info: 'bg-blue-50 border-blue-200 text-blue-800'
};

const toastIcons = {
  success: 'ri-check-circle-line',
  error: 'ri-error-warning-line',
  warning: 'ri-alert-line',
  info: 'ri-information-line'
};

export const Toast: React.FC<ToastProps> = ({ 
  type, 
  message, 
  duration = 3000, 
  onClose 
}) => {
  const [isVisible, setIsVisible] = useState(true);

  useEffect(() => {
    const timer = setTimeout(() => {
      setIsVisible(false);
      setTimeout(onClose, 300); // 等待动画完成
    }, duration);

    return () => clearTimeout(timer);
  }, [duration, onClose]);

  return (
    <div className={`
      fixed top-4 right-4 z-50 max-w-sm w-full
      transform transition-all duration-300 ease-out
      ${isVisible ? 'translate-x-0 opacity-100' : 'translate-x-full opacity-0'}
    `}>
      <div className={`
        flex items-center gap-3 p-4 border rounded-lg shadow-lg
        ${toastStyles[type]}
      `}>
        <i className={`${toastIcons[type]} text-lg flex-shrink-0`}></i>
        <span className="text-sm font-medium flex-1">{message}</span>
        <button 
          onClick={() => setIsVisible(false)}
          className="text-current opacity-70 hover:opacity-100 transition-opacity"
        >
          <i className="ri-close-line text-sm"></i>
        </button>
      </div>
    </div>
  );
};
```

---

## 3. 🎛️ 优化分组管理

### 问题描述
分组功能缺乏拖拽排序、批量操作等高级功能。

### 技术实现

#### 3.1 拖拽排序功能
```bash
# 安装拖拽库
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```

```typescript
// components/DraggableTabGroup.tsx
import React from 'react';
import {
  DndContext,
  closestCenter,
  KeyboardSensor,
  PointerSensor,
  useSensor,
  useSensors,
} from '@dnd-kit/core';
import {
  arrayMove,
  SortableContext,
  sortableKeyboardCoordinates,
  verticalListSortingStrategy,
} from '@dnd-kit/sortable';
import {
  useSortable,
} from '@dnd-kit/sortable';
import { CSS } from '@dnd-kit/utilities';

interface DraggableTabGroupProps {
  groups: TabGroup[];
  onReorder: (groups: TabGroup[]) => void;
}

const SortableGroup: React.FC<{ group: TabGroup }> = ({ group }) => {
  const {
    attributes,
    listeners,
    setNodeRef,
    transform,
    transition,
    isDragging,
  } = useSortable({ id: group.id });

  const style = {
    transform: CSS.Transform.toString(transform),
    transition,
    opacity: isDragging ? 0.5 : 1,
  };

  return (
    <div ref={setNodeRef} style={style} {...attributes}>
      <div className="flex items-center gap-2 p-3 bg-white border border-gray-200 rounded-lg">
        <button 
          {...listeners}
          className="text-gray-400 hover:text-gray-600 cursor-grab active:cursor-grabbing"
        >
          <i className="ri-drag-move-2-line"></i>
        </button>
        <span className="flex-1">{group.title}</span>
        <span className="text-sm text-gray-500">{group.tabs.length} 个标签页</span>
      </div>
    </div>
  );
};

export const DraggableTabGroup: React.FC<DraggableTabGroupProps> = ({
  groups,
  onReorder
}) => {
  const sensors = useSensors(
    useSensor(PointerSensor),
    useSensor(KeyboardSensor, {
      coordinateGetter: sortableKeyboardCoordinates,
    })
  );

  const handleDragEnd = (event: any) => {
    const { active, over } = event;

    if (active.id !== over.id) {
      const oldIndex = groups.findIndex(g => g.id === active.id);
      const newIndex = groups.findIndex(g => g.id === over.id);
      onReorder(arrayMove(groups, oldIndex, newIndex));
    }
  };

  return (
    <DndContext 
      sensors={sensors}
      collisionDetection={closestCenter}
      onDragEnd={handleDragEnd}
    >
      <SortableContext items={groups.map(g => g.id)} strategy={verticalListSortingStrategy}>
        <div className="space-y-2">
          {groups.map(group => (
            <SortableGroup key={group.id} group={group} />
          ))}
        </div>
      </SortableContext>
    </DndContext>
  );
};
```

#### 3.2 分组批量操作
```typescript
// components/GroupBatchActions.tsx
import React from 'react';

interface GroupBatchActionsProps {
  selectedGroups: string[];
  onMergeGroups: () => void;
  onDeleteGroups: () => void;
  onExportGroups: () => void;
}

export const GroupBatchActions: React.FC<GroupBatchActionsProps> = ({
  selectedGroups,
  onMergeGroups,
  onDeleteGroups,
  onExportGroups
}) => {
  if (selectedGroups.length === 0) return null;

  return (
    <div className="fixed bottom-4 left-1/2 transform -translate-x-1/2 z-50">
      <div className="bg-white border border-gray-200 rounded-lg shadow-lg p-3">
        <div className="flex items-center gap-2">
          <span className="text-sm text-gray-600 mr-2">
            已选择 {selectedGroups.length} 个分组
          </span>
          
          {selectedGroups.length > 1 && (
            <button
              onClick={onMergeGroups}
              className="px-3 py-1.5 text-sm bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-colors"
            >
              <i className="ri-merge-cells-horizontal mr-1"></i>
              合并
            </button>
          )}
          
          <button
            onClick={onExportGroups}
            className="px-3 py-1.5 text-sm bg-green-600 text-white rounded-md hover:bg-green-700 transition-colors"
          >
            <i className="ri-download-line mr-1"></i>
            导出
          </button>
          
          <button
            onClick={onDeleteGroups}
            className="px-3 py-1.5 text-sm bg-red-600 text-white rounded-md hover:bg-red-700 transition-colors"
          >
            <i className="ri-delete-bin-line mr-1"></i>
            删除
          </button>
        </div>
      </div>
    </div>
  );
};
```

---

## 4. 🔄 添加撤销/重做功能

### 问题描述
用户误操作后无法撤销，影响使用体验。

### 技术实现

#### 4.1 历史记录管理Hook
```typescript
// hooks/useHistory.ts
import { useState, useCallback } from 'react';

interface HistoryState<T> {
  past: T[];
  present: T;
  future: T[];
}

export const useHistory = <T>(initialState: T) => {
  const [history, setHistory] = useState<HistoryState<T>>({
    past: [],
    present: initialState,
    future: []
  });

  const setState = useCallback((newState: T, actionName?: string) => {
    setHistory(prev => ({
      past: [...prev.past, prev.present],
      present: newState,
      future: []
    }));
  }, []);

  const undo = useCallback(() => {
    setHistory(prev => {
      if (prev.past.length === 0) return prev;
      
      const previous = prev.past[prev.past.length - 1];
      const newPast = prev.past.slice(0, prev.past.length - 1);
      
      return {
        past: newPast,
        present: previous,
        future: [prev.present, ...prev.future]
      };
    });
  }, []);

  const redo = useCallback(() => {
    setHistory(prev => {
      if (prev.future.length === 0) return prev;
      
      const next = prev.future[0];
      const newFuture = prev.future.slice(1);
      
      return {
        past: [...prev.past, prev.present],
        present: next,
        future: newFuture
      };
    });
  }, []);

  const canUndo = history.past.length > 0;
  const canRedo = history.future.length > 0;

  return {
    state: history.present,
    setState,
    undo,
    redo,
    canUndo,
    canRedo
  };
};
```

#### 4.2 撤销/重做工具栏
```typescript
// components/UndoRedoToolbar.tsx
import React from 'react';

interface UndoRedoToolbarProps {
  canUndo: boolean;
  canRedo: boolean;
  onUndo: () => void;
  onRedo: () => void;
}

export const UndoRedoToolbar: React.FC<UndoRedoToolbarProps> = ({
  canUndo,
  canRedo,
  onUndo,
  onRedo
}) => {
  return (
    <div className="flex items-center gap-1 border-r border-gray-200 pr-3 mr-3">
      <button
        onClick={onUndo}
        disabled={!canUndo}
        className={`p-2 rounded-md transition-colors ${
          canUndo 
            ? 'text-gray-700 hover:bg-gray-100' 
            : 'text-gray-300 cursor-not-allowed'
        }`}
        title="撤销 (Ctrl+Z)"
      >
        <i className="ri-arrow-go-back-line"></i>
      </button>
      
      <button
        onClick={onRedo}
        disabled={!canRedo}
        className={`p-2 rounded-md transition-colors ${
          canRedo 
            ? 'text-gray-700 hover:bg-gray-100' 
            : 'text-gray-300 cursor-not-allowed'
        }`}
        title="重做 (Ctrl+Y)"
      >
        <i className="ri-arrow-go-forward-line"></i>
      </button>
    </div>
  );
};
```

---

## 5. 📱 响应式布局优化

### 问题描述
当前布局在小屏幕设备上体验不佳。

### 技术实现

#### 5.1 响应式网格布局
```typescript
// components/ResponsiveTabGrid.tsx
import React from 'react';
import { Tab } from '../types';
import { TabItem } from './TabItem';

interface ResponsiveTabGridProps {
  tabs: Tab[];
  selectedTabs: string[];
  onSelectTab: (tabId: string) => void;
}

export const ResponsiveTabGrid: React.FC<ResponsiveTabGridProps> = ({
  tabs,
  selectedTabs,
  onSelectTab
}) => {
  return (
    <div className="
      grid gap-4
      grid-cols-1
      sm:grid-cols-2
      lg:grid-cols-3
      xl:grid-cols-4
      2xl:grid-cols-5
    ">
      {tabs.map(tab => (
        <TabItem
          key={tab.id}
          tab={tab}
          isSelected={selectedTabs.includes(tab.id)}
          onSelect={() => onSelectTab(tab.id)}
        />
      ))}
    </div>
  );
};
```

#### 5.2 移动端优化的操作栏
```typescript
// components/MobileActionBar.tsx
import React, { useState } from 'react';

interface MobileActionBarProps {
  selectedCount: number;
  onBatchRestore: () => void;
  onBatchDelete: () => void;
  onAddToGroup: () => void;
}

export const MobileActionBar: React.FC<MobileActionBarProps> = ({
  selectedCount,
  onBatchRestore,
  onBatchDelete,
  onAddToGroup
}) => {
  const [isExpanded, setIsExpanded] = useState(false);

  if (selectedCount === 0) return null;

  return (
    <div className="fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 p-4 md:hidden">
      <div className="flex items-center justify-between">
        <span className="text-sm text-gray-600">
          已选择 {selectedCount} 个标签页
        </span>
        
        <button
          onClick={() => setIsExpanded(!isExpanded)}
          className="p-2 text-gray-600 hover:bg-gray-100 rounded-md"
        >
          <i className={`ri-more-line transform transition-transform ${
            isExpanded ? 'rotate-90' : ''
          }`}></i>
        </button>
      </div>
      
      {isExpanded && (
        <div className="mt-3 flex gap-2">
          <button
            onClick={onBatchRestore}
            className="flex-1 py-2 px-3 bg-blue-600 text-white rounded-md text-sm font-medium"
          >
            <i className="ri-refresh-line mr-1"></i>
            恢复
          </button>
          
          <button
            onClick={onAddToGroup}
            className="flex-1 py-2 px-3 bg-green-600 text-white rounded-md text-sm font-medium"
          >
            <i className="ri-folder-add-line mr-1"></i>
            分组
          </button>
          
          <button
            onClick={onBatchDelete}
            className="flex-1 py-2 px-3 bg-red-600 text-white rounded-md text-sm font-medium"
          >
            <i className="ri-delete-bin-line mr-1"></i>
            删除
          </button>
        </div>
      )}
    </div>
  );
};
```

---

## 📊 实施优先级

1. **第一阶段** (1周):
   - 搜索功能增强
   - 状态指示器优化
   - Toast反馈组件

2. **第二阶段** (1-2周):
   - 分组拖拽排序
   - 撤销/重做功能
   - 响应式布局优化

3. **第三阶段** (2-3周):
   - 批量操作完善
   - 移动端体验优化
   - 性能优化和测试

---

## 🎯 预期效果

- **搜索体验提升**：更智能的搜索建议和历史记录
- **操作反馈完善**：用户操作有明确的状态反馈
- **功能更加完整**：支持拖拽、撤销等高级功能
- **移动端友好**：在各种设备上都有良好体验

这些改进将使Tabify成为一个功能完整、体验优秀的标签页管理工具。